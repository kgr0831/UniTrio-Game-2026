# Unitro2 시스템 이식 검증 체크리스트

이 문서는 `Unitro` 씬에서 `Unitro2` 씬으로 이식된 인벤토리, 스킬 매니저, 드래그 앤 드롭 시스템이 정상적으로 작동하는지 확인하기 위한 가이드입니다.

## 1. 에디터 준비 (필수)
> [!IMPORTANT]
> YAML 수정 사항을 반영하기 위해 반드시 아래 절차를 먼저 수행해야 합니다.

- [ ] **Scene Reload**: 유니티 에디터에서 `Unitro2` 씬을 다시 열거나, 다른 씬으로 갔다가 돌아옵니다.
- [ ] **Console 창 확인**: 씬 로드 시 빨간색 에러(`NullReferenceException` 등)가 발생하지 않는지 확인합니다.

## 2. 하이어라키(Hierarchy) 구조 확인
유니티 하이어라키 창에서 다음 오브젝트들이 올바르게 배치되었는지 확인합니다.

- [ ] **Canvas 자식 확인**: `Canvas` 오브젝트 하위에 `InventoryPanel`과 `QuickSlotManager`가 정상적으로 포함되어 있는지 확인합니다.
- [ ] **활성화 상태 확인**: `InventoryPanel` 오브젝트가 하이어라키에서 회색(비활성)이 아닌 **흰색(활성)** 상태인지 확인합니다.
- [ ] **매니저 존재 여부**: 씬 루트 또는 시스템 그룹 하위에 `DragManager`가 존재하는지 확인합니다.

## 3. 인스펙터(Inspector) 설정 확인
각 오브젝트를 클릭하여 인스펙터 창의 참조가 깨지지 않았는지 확인합니다.

- [ ] **InventoryPanel**: `InventoryGenerator` 컴포넌트가 붙어 있고, `Slot Prefab`이나 `Container` 참조가 깨져 있지 않은지(`Missing` 표시 여부) 확인합니다.
- [ ] **QuickSlotManager**: 스크립트의 `Slots` 리스트에 하위 퀵슬롯 오브젝트들이 자동으로 연결되어 있는지 확인합니다.
- [ ] **DragManager**: `Drag Visual Icon` 참조가 캔버스 내의 해당 아이콘 오브젝트(`dragVisualIcon`)로 연결되어 있는지 확인합니다.

## 4. 런타임(Play Mode) 기능 테스트
게임을 실행하여 실제 기능이 작동하는지 확인합니다.

- [ ] **인벤토리 열기**: 플레이 모드 진입 후 단축키(주로 `I`)를 눌렀을 때 인벤토리 UI가 정상적으로 나타나는지 확인합니다.
- [ ] **아이템 생성**: `InventoryGenerator`에 의해 슬롯들이 자동으로 생성(Runtime Generation)되는지 확인합니다.
- [ ] **드래그 앤 드롭**:
    - [ ] 인벤토리의 아이템/스킬을 마우스로 클릭하여 드래그할 때 아이콘이 마우스 포인터를 따라오는지 확인합니다.
    - [ ] 드래그한 아이템을 퀵슬롯 위에 놓았을 때 해당 슬롯에 아이콘이 안착하는지 확인합니다.
- [ ] **기능 연동**: 퀵슬롯에 등록된 스킬/아이템을 단축키(1, 2, 3...)로 사용했을 때 작동하는지(또는 콘솔 로그가 찍히는지) 확인합니다.

## 5. 통합 기능 유지 확인 (Side Effects)
- [ ] **사망 애니메이션**: 캐릭터 사망 시 사망 애니메이션이 여전히 정상적으로 출력되는지 확인합니다.
- [ ] **맵 생성(MapGen)**: 초기 맵 생성이 이전과 동일하게 유지되는지 확인합니다.

---
> [!TIP]
> 위 항목 중 하나라도 작동하지 않는다면, 해당 오브젝트의 이름을 포함하여 제게 알려주세요. 즉시 해결해 드리겠습니다.
